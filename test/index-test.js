const expect = require('expect');
const fs = require('fs');
const jsdom = require('jsdom');
const path = require('path');

describe('index', () => {
  
  it('does not commit token', () => {
    expect(0).toEqual(0);
  });
});
